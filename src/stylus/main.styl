body, html {
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  overflow: hidden;
  font-size: 14px;
  font: 10pt "Helvetica Neue", Arial, Helvetica, sans-serif;
}

.mapInfoWidget {
  position: absolute;
  bottom: 0;
  left: 0;
  z-index: 50;
}
